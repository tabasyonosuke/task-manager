# Task Manager

Laravelで作成したシンプルなタスク管理アプリです。

## 実装済み機能

- Blade の構文エラー修正 (@if($tasks->isEmpty()))
- ローカル環境での動作確認済み
- TaskController に toggleComplete メソッドを追加
- タスク作成機能の実装
- タスク削除機能の実装
- 419エラーおよびルート名の不整合を修正
- タスク作成機能の実装（詳細・期限の同時入力に対応）
- タスク編集機能（edit/update）の実装
- データベースへ詳細（description）と期限（due_date）カラムを追加
- 一覧画面のデザイン刷新と詳細・期限の表示対応

**2026-01-23**
- 認証機能（新規登録・ログイン・ログアウト）のスクラッチ実装
- tasksテーブルへuser_idカラムを追加し、ユーザーとタスクを紐付け（Migration）
- UserモデルとTaskモデルのリレーション設定（1対多）
- Authミドルウェアによるタスク一覧へのアクセス制限
- ログインユーザーのみのタスクを表示・保存するようTaskControllerを修正
- 共通レイアウトにログイン/ログアウト切り替えのナビゲーションを追加

**2026-01-24**
- ソート機能（作成順・期限順）の実装
- フィルター機能（すべて・未完了のみ）の実装
- 期限順ソート時に期限なしタスクを後回しにするロジックの追加
- 未完了のみ表示時に完了済みセクションを非表示にするUI制御の実装

**2026-02-06**
- TypeScript 開発環境の構築（tsconfig.json, Vite設定の変更）
- 入力フォームのリアルタイム・バリデーション実装（TypeScript）
    - 20文字制限の監視とエラーメッセージの動的表示
    - バリデーションエラー時の送信ボタン無効化制御
- 非同期通信（Ajax）によるタスク完了切り替えの基盤実装
    - Fetch API と async/await を用いたリロードレスな通信ロジック
    - CSRFトークンのメタタグ経由での取得とリクエストヘッダーへの付与
- 共通レイアウト（app.blade.php）への Ajax 通信用メタタグ追加
